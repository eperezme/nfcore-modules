params {
    duplex = null    //false == "basecaller", true == "duplex" 
    

    // Emit options
    emit_fastq = false
    emit_bam = false

    // Trimming options
    demultiplexing = false
    kit = null
    trim = "none" // "none", "all", "primers", "adapters"

    // Basecalling options
    model = "fast" // "fast", "hac", "sup", or specific model "dna_r10.4.1_e8.2_400bps_fast@v5.0.0"
    modified_bases = null

    // Alignment options
    align = false
    ref_genome = null
    kmer_size = 15
    win_size = 5


    //Aditional args
    error_correction = false

}